/* 
    reads an integer from a char array
    writes back this integer to an provides address,
        if the char array is of sufficient format
    returns 0, if an integer is written back
 */
int svs_atoi(char*,int*);
